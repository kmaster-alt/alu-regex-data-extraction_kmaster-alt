# Regex Data Extraction

This project uses Python regex to extract and validate data from a raw text file.

The program extracts:
- ALU email addresses
- URLs
- Phone numbers
- Credit card numbers

Invalid or unsafe text is ignored.

Credit card numbers are masked for security.

## Run the Program

```bash
py src/main.py
